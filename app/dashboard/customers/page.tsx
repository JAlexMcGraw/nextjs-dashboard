import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Customers Page",
    description: "Where to find info on the customers",
}

export default function Page() {
    return <p>Customers Page</p>;
}